import reactCSS from "reactcss";

export const styles = reactCSS({
  default: {
    righticons: {
      border: "none",
      width: `${window.innerWidth * 0.03}px`,
      height: `${window.innerHeight * 0.1}px`,
      background: "none",
      borderRadius: "0.1%",
      outline: "none",
      padding: "0.5%",
    },
  },
});
